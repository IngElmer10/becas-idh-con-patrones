RewriteEngine On

# Si existe archivo o carpeta, servirlo directo
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Todo lo demás pasa por el front controller
RewriteRule ^ index.php [QSA,L]

